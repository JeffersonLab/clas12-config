name: mcdj

on:
  pull_request:
  push:
    branches: [ main ]
    tags: [ '*' ]

jobs:
  mcgen:
    runs-on: ubuntu-latest
    steps:
      - uses: docker://codecr.jlab.org/hallb/clas12/container-forge/base_root:latest
      - uses: actions/checkout@v6
      - name: json
        run: ./mcdj -h
