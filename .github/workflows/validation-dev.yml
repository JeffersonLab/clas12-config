name: Validation-dev

on:
  pull_request:
    branches: [ dev ] # PRs which target `dev`
  push:
    branches: [ dev ] # pushes on `dev` (and merging PRs to `dev`)
  workflow_dispatch: # manual trigger

jobs:
  validation:
    uses: gemc/clas12Tags/.github/workflows/experiment_test.yml@main
    